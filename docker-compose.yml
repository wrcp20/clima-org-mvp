services:
  api:
    build: ./api
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    env_file: .env
  dashboard:
    build: ./dashboard
    ports:
      - "8501:8501"
    env_file: .env
    depends_on:
      - api
